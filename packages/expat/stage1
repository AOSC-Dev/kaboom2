#!/bin/bash
set_trap

# Strange ... the project lives in a subdirectory.
pushd "$PKGNAME".git/expat
abinfo "Regenerating configure ..."
autoreconf -fvis

md build-"$KABOOM_TARGET_ARCH"

configure_native2
make_build
make_install_native
