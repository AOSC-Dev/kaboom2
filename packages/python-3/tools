#!/bin/bash
set_trap
pushd Python-$PKGVER
md build-"$KABOOM_HOST_TRIPLE"

configure_tools2 \
	--with-ensurepip
make_build
make_install_tools

abinfo "Installing host modules ..."
python3 -m pip install --upgrade \
	pip wheel pep517 platformdirs packaging click installer build
popd
