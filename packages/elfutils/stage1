#!/bin/bash
set_trap

pushd "$PKGNAME"-"$PKGVER"

md build-"$KABOOM_TARGET_ARCH"

configure_native2 \
	--program-prefix=eu- \
	--disable-install-elfh \
	--disable-debuginfod
make_build
make_install_native
