#!/bin/bash
set_trap

pushd "$PKGNAME"-"$PKGVER"
md build-"$KABOOM_TARGET_ARCH"

# LFS: Workaround a bug involving cross-compilation.
configure_native2 \
	gl_cv_func_strcasecmp_works=y
make_build
make_install_native
